# DEPENDENCIES Fix Plan

## Changes

- `requirements.txt`:
  - Added `psutil>=5.9.0` to lock dependencies for active window tracking.

## Verification goals

- [x] All runtime imports accounted for in `requirements.txt`
- [x] All dependencies verified on official PyPI registry
